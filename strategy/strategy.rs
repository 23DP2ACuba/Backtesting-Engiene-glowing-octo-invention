use std::collections::HashMap;
use indicators::Indicator;
use crate::data::Data;
use engnine::Engine;

pub mod strategy {
    struct Prams {
        data: HashMap<String, _>,

    }
    impl Prams {
        
    }
}